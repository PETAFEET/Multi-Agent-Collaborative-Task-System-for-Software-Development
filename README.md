# 多智能体协作任务系统 (Multi-Agent Collaboration System)

基于 LangChain 构建的分布式多智能体协作框架，支持任务自动分解、角色分配、执行监督和结果集成。

## 🚀 功能特性

### 核心功能

- **多智能体协作**: 支持规划、执行、监督等多种专业智能体
- **任务分解**: 自动将复杂任务分解为可执行的子任务
- **实时监控**: Web 界面实时展示智能体状态和协作过程
- **结果集成**: 自动汇总各智能体输出生成最终结果

### 高级功能

- **MCP 集成**: 支持 Model Context Protocol 服务器集成
- **浏览器操作**: 集成 Browser Use 框架支持 GUI 操作
- **异步执行**: 支持后台异步任务执行
- **可扩展架构**: 易于添加新的智能体类型

## 🏗️ 系统架构

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   用户界面      │    │   中央控制器    │    │   任务队列      │
│   (Web UI)      │◄──►│  (Coordinator)  │◄──►│   (Queue)       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │
                    ┌─────────┼─────────┐
                    │         │         │
            ┌───────▼───┐ ┌───▼───┐ ┌───▼───┐
            │ 规划智能体 │ │执行智能体│ │监督智能体│
            │(Planner)  │ │(Executor)│ │(Monitor)│
            └───────────┘ └────────┘ └────────┘
```

## 📁 项目结构

```
multi-agent-project/
├── src/
│   ├── agents/              # 智能体实现
│   │   ├── base.py         # 基础智能体类
│   │   ├── planner.py      # 规划智能体
│   │   ├── executor.py     # 执行智能体
│   │   ├── monitor.py      # 监督智能体
│   │   └── browser.py      # 浏览器智能体
│   ├── core/               # 核心框架
│   │   ├── coordinator.py  # 中央控制器
│   │   ├── task_manager.py # 任务管理器
│   │   ├── communication.py # 通信模块
│   │   └── logger.py       # 日志系统
│   ├── integrations/       # 外部集成
│   │   ├── mcp_client.py   # MCP客户端
│   │   └── browser_use.py  # 浏览器操作
│   ├── web/                # Web界面
│   │   ├── app.py          # Flask应用
│   │   ├── templates/      # HTML模板
│   │   └── static/         # 静态资源
│   └── utils/              # 工具函数
│       ├── config.py       # 配置管理
│       └── helpers.py      # 辅助函数
├── examples/               # 示例任务
│   ├── travel_planning.py  # 旅行规划示例
│   ├── code_development.py # 代码开发示例
│   └── research_report.py  # 研究报告示例
├── tests/                  # 测试文件
├── requirements.txt        # 依赖包
├── config.yaml            # 配置文件
└── README.md              # 项目说明
```

## 🛠️ 技术栈

- **框架**: LangChain, FastAPI/Flask
- **AI 模型**: OpenAI GPT, Anthropic Claude, 或其他兼容模型
- **前端**: React/Vue.js + WebSocket
- **数据库**: SQLite/PostgreSQL
- **任务队列**: Celery + Redis
- **浏览器自动化**: Browser Use, Selenium
- **MCP 集成**: Model Context Protocol

## 🚀 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置环境

```bash
cp config.yaml.example config.yaml
# 编辑配置文件，添加API密钥等
```

### 3. 启动系统

```bash
# 启动Web界面
python -m src.web.app

# 启动任务队列
celery -A src.core.task_manager worker --loglevel=info
```

### 4. 访问界面

打开浏览器访问 `http://localhost:5000`

## 📖 使用示例

### 旅行规划任务

```python
from src.core.coordinator import MultiAgentCoordinator

# 创建协调器
coordinator = MultiAgentCoordinator()

# 提交任务
task = {
    "type": "travel_planning",
    "description": "规划一次7天的日本东京之旅",
    "requirements": {
        "budget": 10000,
        "interests": ["文化", "美食", "购物"],
        "dates": "2024-03-15 to 2024-03-22"
    }
}

result = await coordinator.execute_task(task)
print(result)
```

## 🔧 配置说明

### 智能体配置

```yaml
agents:
  planner:
    model: "gpt-4"
    temperature: 0.7
    max_tokens: 2000

  executor:
    model: "gpt-3.5-turbo"
    temperature: 0.3
    max_tokens: 1000

  monitor:
    model: "gpt-4"
    temperature: 0.5
    max_tokens: 1500
```

### MCP 集成配置

```yaml
mcp:
  enabled: true
  servers:
    - name: "file_system"
      url: "mcp://localhost:3000"
    - name: "web_search"
      url: "mcp://localhost:3001"
```

## 🧪 测试

```bash
# 运行所有测试
pytest tests/

# 运行特定测试
pytest tests/test_agents.py

# 运行集成测试
pytest tests/test_integration.py
```

## 📝 开发指南

### 添加新智能体

1. 继承 `BaseAgent` 类
2. 实现 `execute` 方法
3. 在配置文件中注册智能体
4. 编写测试用例

### 添加新任务类型

1. 在 `task_manager.py` 中定义任务类型
2. 实现任务分解逻辑
3. 配置智能体协作流程
4. 添加示例和测试

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License
